#include<stdio.h>

int main()
{
	char str[4];
	int i;

	printf("Enter a three digit number");
	scanf_s("%s", str, 4);

	for (i = sizeof(str) - 1;i >= 0;i--)
	{
		printf("%c", str[i]);
	}
}